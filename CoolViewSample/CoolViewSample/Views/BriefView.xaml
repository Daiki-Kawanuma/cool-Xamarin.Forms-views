<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CoolViewSample.Views.BriefView">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="labelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="White" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentView.Content>
        <StackLayout BackgroundColor="#33FFFFFF">
            <Label Text="{Binding Date}" Style="{StaticResource labelStyle}" FontAttributes="Bold" HorizontalOptions="Center"/>
            <Image Source="{Binding Icon}" HorizontalOptions="Center" Margin="5"/>
            <Image Source="{Binding Category1st}" Margin="5,15,5,5"/>
            <Image Source="{Binding Category2nd}" Margin="5" />
            <Image Source="{Binding Category3rd}" Margin="5" />
        </StackLayout>
    </ContentView.Content>
</ContentView>