<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CoolViewSample.Views.DetailView">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="labelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="White" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentView.Content>
        <StackLayout>
            <Label Grid.Column="1" Text="{Binding Name}" FontSize="Large" TextColor="White" />
            <WebView Source="{Binding BlogUrl}" VerticalOptions="FillAndExpand"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>